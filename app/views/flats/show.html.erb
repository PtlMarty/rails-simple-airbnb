<div class="container">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @flat.name %></h5>
      <p class="card-text"><%= @flat.description %></p>
      <%= link_to "Back", flats_path %>
  </div>
</div>
